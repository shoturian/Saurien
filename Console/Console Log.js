//New Bot 
//
//Don't Ready For Use

const chalk = require('chalk');
const moment = require('moment');
const Discord = require('discord.js');
const token = require('../token.json');

var botname = token.botname;
var release = token.release;
var dev = token.dev;
var pre-dev = token.pre-dev

module.exports = NewID => {
  console.log(`[${moment().format('YYYY-MM-DD HH:mm:ss')}] Activating The Bot!`);
  console.log(`[${moment().format('YYYY-MM-DD HH:mm:ss')}] Bot Activated!`);
  console.log(`[${moment().format('YYYY-MM-DD HH:mm:ss')}] Bot Devloper ${dev} and ${pre-dev}`);
  console.log(`[${moment().format('YYYY-MM-DD HH:mm:ss')}] ${botname} Release ${surum} By ${dev} and ${pre-dev}`);
				  
				  
};
